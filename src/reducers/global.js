const initialState={

}

export default function(state=initialState,action){
    const type=action.type;
    switch(type){
        case '':

        break;
        default:
            return state;
    }
}